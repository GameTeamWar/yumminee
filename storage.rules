rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow all operations for testing
      allow read, write, create, update, delete;
    }
  }
}