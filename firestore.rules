rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode - allow all authenticated operations
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}